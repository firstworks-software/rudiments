// Copyright (c) David Muse
// See the file COPYING for more information

#include <rudiments/dh.h>
#include <rudiments/bytestring.h>
#include <rudiments/stdio.h>
#include "test.cpp"

// RFC 2409 Oakley group 2 (1024-bit MODP) prime and its generator (2)
static const byte_t	prime[]={
	
};
static const byte_t	generator[]={ 0x02 };

int main(int argc, const char **argv) {

	header("dh");

	dh	alice;
	if (!alice.isSupported()) {
		stdoutput.printf("not supported\n\n");
		return 0;
	}

	dh	bob;

	// both sides agree on the same group
	alice.setPrimeModulus(prime,sizeof(prime));
	alice.setGenerator(generator,sizeof(generator));
	bob.setPrimeModulus(prime,sizeof(prime));
	bob.setGenerator(generator,sizeof(generator));

	// each side generates a key pair
	test("alice keys",alice.generateKeys());
	test("bob keys",bob.generateKeys());
	test("alice public key",alice.getPublicKeySize()>0);
	test("bob public key",bob.getPublicKeySize()>0);

	// each side receives the other's public key
	alice.setPeerPublicKey(bob.getPublicKey(),bob.getPublicKeySize());
	bob.setPeerPublicKey(alice.getPublicKey(),alice.getPublicKeySize());

	// each side computes the shared secret
	test("alice secret",alice.generateSharedSecret());
	test("bob secret",bob.generateSharedSecret());

	// the whole point: both sides arrive at the same shared secret
	test("secrets match",
		alice.getSharedSecretSize()>0 &&
		alice.getSharedSecretSize()==bob.getSharedSecretSize() &&
		!bytestring::compare(alice.getSharedSecret(),
					bob.getSharedSecret(),
					(size_t)alice.getSharedSecretSize()));

	stdoutput.printf("\n");

	return 0;
}
